#include "so_long.h"
/*
void	game_window(t_data *app)
{
	int	i;

	i = 0;
	while(app->map[i] != NULL)
		i++;
	x = ;
	
	
	app->win2 = mlx_new_window(app->mlx, 
		(ft_strlen(app->map[0]) - 1), (i - 1), "Snake");
	if (!app->win2)
		destroy_game(app);
	create_map(app);
}

*/